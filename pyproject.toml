[build-system]
requires = ["setuptools>=68", "setuptools-scm>=8", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pirt-camera-control"
dynamic = ["version"]
description = "TCP/IP control interface for PIRT scientific camera with GUI and client library"
readme = "README.md"
requires-python = ">=3.8"
license = {text = "MIT"}
authors = [
    {name = "Kishalay De"},
    {name = "Nate Lourie"},
]

keywords = ["camera", "astronomy", "scientific-instrumentation", "fits", "tcp-ip"]


dependencies = [
    "numpy",
    "astropy",
    "PyQt5",
    "matplotlib",
    "pyqtgraph",
    "Pyro5",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "black",
    "flake8",
    "mypy",
    "pre-commit",
    "ipykernel",
    "ruff",
]
docs = [
    "sphinx>=5.0",
    "sphinx-rtd-theme>=1.0",
]
examples = [
    "ipython>=8.0",
    "jupyter>=1.0",
]


[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools_scm]
version_scheme = "release-branch-semver"
local_scheme = "node-and-date"
write_to = "src/pirtcam/_version.py"
